# 代码审查报告 — Container Corridor Benchmark

对 `container_sim.py` / `coordination.py` / `viz.py` 的一次系统性审查，包含问题诊断、修复实现，以及 seed 12 的复现结果。

审查方式：先把整套代码跑起来验证行为（baseline 死锁、COORD 存活与文档一致），再做静态审查 + 针对性运行期探测，确认每一处疑点。

---

## 一、整体评估

架构是扎实的：分层清晰（A\* 全局规划 + ORCA 局部避障 + 可选协调层）、参数化合理、确定性可复现、文档完善。**没有发现会让基准结果失效的严重 bug** —— 文档报告的"baseline 死锁、COORD 撑满 5000 步"我都复现到了。

发现的问题集中在两类：**死代码**（写了但从不使用，删除零风险）和 **一处潜在崩溃隐患**（retreat 索引越界）。下面按严重程度分级。

---

## 二、问题清单与修复

### 已修复

| # | 文件 | 类型 | 问题 | 修复 |
|---|------|------|------|------|
| 1 | coordination.py | 死代码 | `reset_robot()` 从未被调用，且只初始化 3 个 `coord_*` 字段，与 `Robot.__init__` 的 11 个字段不一致（若被调用会触发 `AttributeError`） | 删除该方法 |
| 2 | 两个文件 | 死字段 | `coord_dyn_yield` 被赋值 2 次但从未被读取（retreat 目标实际走 `_holder_path` + `_retreat_idx`） | 删除字段定义及全部赋值 |
| 3 | coordination.py | 无用 import | `from collections import deque` 未使用（deque 的实际使用在 container_sim.py） | 删除该 import |
| 4 | coordination.py | 文档错误 | `has_crossed` 的 docstring 称"在另一侧**且**清出 bottleneck zone"，但实现只判断 side；clearance 检查实际在调用处 | 订正 docstring，注明只判断 side |
| 5 | container_sim.py | **潜在崩溃** | retreat 过程中机器人若到达目标会换目标 + `plan_path` 重置 `path`/`wp_idx`，但 `_retreat_idx` 仍指向**旧 path** 的索引 → 下一 tick 用旧索引查新路径，可能越界 `IndexError` | (a) retreat 分支取 target 前 `clamp` 到 `len(path)-1`；(b) `plan_path` 末尾清空 `_retreat_idx`，使其随新路径一起失效 |
| 8 | container_sim.py | 死变量 | release scheduler 里 `already_out` 计算后从未使用 | 删除 |

### #5 详解（唯一值得马上修的隐患）

机器人退让时沿自己原路径往回退几步，用 `_retreat_idx` 记住"退到路线上第几个点"。问题在于：退让途中若恰好到达当前目标点，程序会立即换新目标并重新规划一条**全新的、可能更短的路径**，但 `_retreat_idx` 没跟着更新，仍指向旧路线的编号。下一刻拿旧编号去查新路径，就可能指到错误的点甚至越界。

> 比喻：你按"倒退三步"往回走，结果有人趁机把脚下的地图整个换掉了——"第三步"在新地图上要么是个莫名其妙的位置，要么根本不存在。

实测当前未崩溃，是因为退让中的机器人在等待、很少恰好触发换目标——但这是埋着的雷。

修复代码：
```python
# (a) retreat 分支，取 target 前 clamp（container_sim.py step_world 内）
if not hasattr(r, "_retreat_idx") or r._retreat_idx is None:
    r._retreat_idx = max(0, r.wp_idx - 1)
r._retreat_idx = min(r._retreat_idx, len(r.path) - 1)   # #5

# (b) plan_path 末尾，使索引随新路径失效
robot.wp_idx = 0
robot._retreat_idx = None   # #5
```

### #11 跟随器自激振荡 —— 接近目标时无法精确到位，导致假死锁（新发现，待修复）

**现象**：seed 12 中第三个机器人（r2，红色）在 baseline 和 COORD 两版**都是 0 任务**，从未完成过一次取放。

**诊断过程**：
1. 追踪 r2 在 COORD 版的状态分布 —— 89% 时间在"自由移动"，让行（retreat）仅 6.5%，排除"被持续让行饿死"的假设。
2. 追踪目标距离 —— r2 卡在 `(3.20, 3.00)`，`v=0.55`（电机在输出速度）但位置 5000 步纹丝不动；离目标还有 2.6m，从未接近。
3. 抓锁前 250 步轨迹 —— **实际走过路程 5.14m，净位移仅 0.004m**，即原地兜圈；朝向 `theta` 在 -60° 到 -140° 之间反复摆动；同期最近邻居稳定在 ~2.0m 外，**无任何机器人贴近**。
4. **单机器人对照实验** —— 地图上只放 r2、移除所有邻居，它仍在 step 431 卡死、0 任务，卡在离目标 0.58m 处。**铁证：与多机器人、与协调层无关。**

**根因**：`_steer_arch1`（stop-turn-go 跟随器）的滞回逻辑（`ENTER_TURN=30°` / `EXIT_TURN=10°`）在"接近目标 + 目标方向与朝向成特定夹角"时形成极限环（limit cycle）：
> 原地转向 → 误差降到 <10° 切换为前进 → 因离目标近，前进一步使朝向误差又 >30° → 重新进入原地转向 → 转过头 → 循环往复。

机器人电机持续输出（一直在动），但全在原地兜圈，净位移趋零，最终被死锁检测（250 步净位移 < `PROGRESS_EPS=0.05`）判定为 locked。

**关键结论**：
- 这是**控制器自身的 bug，不是多机拥堵死锁**，协调层的 jam 检测（只处理"两机器人互相顶牛"）无从介入 —— 这正是为什么开不开 COORD，r2 都是 0 任务。
- 它污染了基准的解读：CHANGELOG 称 arch1 "all runs end in genuine deadlock"，但其中混入了一部分由控制器振荡造成的**假死锁**，并非全是真正的走廊拥堵死锁。

**修复方向（接近目标时放宽朝向死板度）**：离目标较近（如 < 1m）时放宽前进的进入容差 / 给角速度加阻尼，避免"迈一步就歪、歪了就掉头"的怪圈。修复后该机器人可正常完成任务，协调层的真实效果也能更干净地显现。

> 简言之：机器人**快到终点时太"较真"方向**，差一点点就停下掉头，结果在原地打转永远到不了 —— 与其它机器人和协调层都无关，是它自己跟自己较劲。

**修复实现（已完成）**：在 `_steer_arch1` 中引入 `NEAR_TARGET_DIST=1.0`。当机器人距当前目标 < 1m 时：(a) 抑制进入原地转向状态（除非误差 >120° 的近乎掉头），改为边走边转（arc）切入；(b) 给一个最低前进速度，保证位置持续推进、极限环无法形成。

**修复验证**：
- 单机器人对照实验：修复前 step 431 卡死、0 任务；修复后 step 174 完成 3 任务。极限环消除。
- seed 12 红色机器人（r2）：baseline 0→**4 任务**，COORD 0→**11 任务**，彻底正常工作。
- seed 12 整体（见下表对比）：baseline 死锁前完成任务 4→**20**，COORD 总任务 12→**23**。证实之前 arch1 的"genuine deadlock"中混入了控制器振荡造成的假死锁，修复后基准数字整体抬升、协调层真实效果更干净。

---

## 二之二、死锁攻坚（seed 12 专项迭代）

保留 seed 12（双瓶颈难图）作为靶子，逐步让协调层从"空转"变为"真正有用"。本节按发现顺序记录，每一步都由实测数据驱动。

### 起点诊断：协调层在空转

COORD 版 seed 12 表面上撑满 5000 步，但深挖发现：**5000 步里有 45% 的时间（2254 步）有机器人在退让，最终三个机器人仍全部锁定**。能跑满纯属锁定发生晚 + 撞上步数上限，不是调度救活了系统。抓 jam 现场发现**令牌持有者（holder）自己 `v=0` 也卡死了**：它想朝目标走，但退让中的邻居在 ORCA 眼里仍是障碍、仍对它产生斥力，把它推往远离目标的方向（实测正常 ORCA 给 0.25 速度且方向偏 -120°，目标却在 165°）。退让做了无用功。

### 改进 A：令牌持有者降级斥力（`container_sim.py`，用户提出的核心思路）

**机制**：机器人持有 jam 令牌（`coord_dyn_hold=True`）时，ORCA 不再用完整的动态安全余量（1.5×）对待其他机器人，而是把它们当作**近物理尺寸的小障碍**（`STATIC_CLEAR + ROBOT_RADIUS + 0.1 ≈ 1.25m` 间隙）。这样它能穿过让路者开出的缝，又不会真撞上。墙/静态障碍仍正常避让。

> 演进：最初版本是"完全无视其他机器人斥力"，实测出现碰撞（holder 直冲撞上没及时让开的机器人）；改为"降级斥力"后碰撞消除。

**效果**：实测无视斥力时 holder 速度 0.25→0.50、方向从 -120° 修正到 165°（正对目标）。seed 12 任务 23→**31**，死锁从 1514 步推迟到 4016 步。

### 改进 B：令牌轮换（`coordination.py`）

**问题**：原令牌机制选一个 holder 后靠 commitment locking 一直持有，**没有"通过后释放、换下一个"的机制**。三方僵局下放过一个、另外两个还顶着，jam 永不消失。

**机制**：holder 一旦拉开与 jam 质心的距离超过 `JAM_CLEAR`，立即释放令牌（`coord_wait` 归零、退出持有），让剩余竞争者按等待时间接棒。三个机器人因此能依次通过。

**效果**：seed 12 任务 31→**36**，死锁推迟到 4937 步（差 63 步满程）。机器人不再全挤底部——两个成功穿过上方瓶颈跑到地图上半部。

### #12 软锁定恢复 / 原地重启（`container_sim.py`，用户提出）

**问题**：机器人一旦判定 locked 就**永久**变成静态障碍，再也不动——这是设计浪费，也是死锁无法自愈的根本。

**机制**：locked 改为"软锁定"——睡眠 `RECOVER_SLEEP=60` 步（期间仍是静态障碍，防撞），倒计时归零后**原地复活、重新规划路径、重试原目标**。醒来时邻居可能已挪开，僵局自然解开。连续失败 `HARD_LOCK_ATTEMPTS=8` 次才算真死锁；完成一个任务则重置恢复配额。`run_sim` 终止判定相应改为"全员**硬**锁定才算 all_locked"。

**效果**：seed 12 **首次彻底不死锁，5000 步满程**（COORD 37 / baseline 39 任务）。

### #13 低效死锁判据（`container_sim.py`，针对"明显死锁却没检测到"）

**问题（用户敏锐发现）**：软锁定后仍有机器人长时间停滞却不被判死锁。根因是原判据**只看净位移**（起终点直线距离），不看走了多少冤枉路。机器人在"退让→返回→再退让"往返中能凑出零星净位移（如 0.35m）骗过 0.05m 阈值，于是一边原地兜圈（250 步实走 9.39m 路程！）一边逃过检测，永远卡在低效状态——这正是 COORD 当时干不过 baseline 的元凶之一。实测三个机器人各有 178~266 步处于"停滞但未判死锁"。

**机制**：新增低效判据——250 步窗口内若**走过路程 > 2m 但净位移 < 0.6m 且净位移 < 12%×路程**（即在原地兜圈/振荡），也判定为死锁。与原净位移判据是"或"关系。

**效果（关键反转）**：COORD **首次反超 baseline**。COORD 5000 步存活、31 任务；baseline 反而在 1374 步**碰撞**退出（机器人复活后失去协调层管控、乱冲相撞）。证明低效判据 + 协调层是配套的：判据揪出兜圈机器人触发恢复，协调层保证复活后有序通行不相撞。

### 三项改进的逻辑链

1. **令牌降级斥力**（用户）→ holder 不再被让路者卡死
2. **令牌轮换** → 三方僵局能依次疏解
3. **软锁定恢复**（用户）→ 死锁可自愈，seed 12 首次满程
4. **低效死锁判据** → 揪出兜圈漏检，**协调层价值终于显现（反超 baseline）**

### #14 进展看门狗 —— 保证一切死锁都被检测（零漏检重构）

**目标（用户提出）**：保证一切死锁都能被检测到，不能再有"明明卡住、系统却没反应"的漏检。

**漏检系统扫描**：先量化问题。判据"机器人 500 步（50 秒）内位移 < 0.3m 却没被判 locked"，跨 8 个 seed 统计漏检步数合计 **39570 步**——漏检是普遍现象，不止 seed 12。最严重的 seed 23 漏检 12578 步、总任务仅 2（机器人几乎全程卡着却从没被检测）。

**根因**：旧判据有太多"豁免出口"（`waiting` / `coord_dyn_hold` / 原地转向豁免 / 净位移阈值），每个出口都是漏检口子。尤其净位移判据只看起终点直线距离，机器人在退让往返中凑出零星净位移即可骗过阈值。

**重构机制**：用进展看门狗替换旧判据，**零豁免**，双腿并行：
- **第一条腿（靠近目标）**：跟踪机器人对当前目标曾达到的最近距离 `best_goal_dist`；每步若未能再缩短 `PROGRESS_DELTA=0.15m` 则 `stall_steps` 累加，有真实进展则清零；`stall_steps` 达到 `STALL_TIMEOUT=300` 步即判死锁。任何状态（退让/转向/持令牌）都不豁免——真在解决问题的机器人迟早会更靠近某目标，假装忙碌的（兜圈/振荡/被 ORCA 卡住）则永远进展不了。
- **第二条腿（绝对净位移硬底线）**：`pos_long`（300 步长窗口）内净位移 < 0.5m 即判死锁。防止第一条腿被"缓慢漂移偶尔蹭到目标"的假进展波动骗过。两条腿是"或"关系。
- 换目标 / 复活 / 触发死锁时一并重置 `best_goal_dist`、`stall_steps`、`pos_long`。

**验证（零漏检确认）**：用真实 `run_sim` 跑 12 个 seed，检查是否存在"跑满 5000 步却任务极少"的隐藏空耗死锁——**结果无一例**。所有真正卡死的机器人最终都被 `all_locked` 或 `collision` 正常捕获，没有静默卡死逃逸。早期扫描报出的"残留漏检"经逐案排查，均为扫描标准误判（龟速蹭目标完成任务、正处于软锁恢复周期、或已达 `HARD_LOCK_ATTEMPTS` 上限的硬锁定——这些都是"已检测"状态，非漏检）。

> **结论**：零漏检目标达成。进展看门狗以"是否朝目标推进"这一唯一本质作为判据，堵上了所有豁免口子。

**副作用（留待下一阶段）**：检测变灵敏 + 频繁软锁恢复后，12 个 seed 中有 4 个（含 seed 12）出现 `collision`——机器人复活后缺乏复活时刻的防撞协调，挤在一起相撞。这是检测重构暴露出的、独立的"恢复安全"问题，下一阶段处理（复活时防撞 / 错峰复活）。

### 三项改进的逻辑链

1. **令牌降级斥力**（用户）→ holder 不再被让路者卡死
2. **令牌轮换** → 三方僵局能依次疏解
3. **软锁定恢复**（用户）→ 死锁可自愈，seed 12 首次满程
4. **低效死锁判据** → 揪出兜圈漏检，协调层价值显现
5. **#14 进展看门狗**（用户）→ 零豁免检测，保证一切死锁都被捕获

### #15 恢复期调优 —— 延长冻结睡眠 + 令牌车可穿越冻结车（针对 #14 暴露的复活碰撞）

**背景**：#14 零漏检重构后，检测更灵敏、软锁恢复更频繁，复活时缺乏防撞协调，12 个 seed 中 4 个出现碰撞，seed 12 在 2199 步碰撞退出。本节按用户提出的两点思路处理"恢复安全"。

**碰撞根因复现**：抓 seed 12 碰撞现场发现，撞墙者 r1 正缓慢蠕动趋向一个 `x=4.3` 的目标，而右墙离散为半径 0.25 的圆盘、圆心在 `x=5.0`。目标采样器把墙当作直线、用 `STATIC_CLEAR=0.65` 留白（允许 `x≤4.35`），但碰撞判据把墙当圆盘、要求圆心间距 `≥ R+wall_r-0.05 = 0.70`。目标 `x=4.3` 距墙圆心恰好 0.70——卡在阈值上。近目标蠕动逻辑（#11）持续把它往目标推，越过这条发丝般的余量，蹭上墙判为碰撞。属采样器与碰撞判据的"墙模型不一致"（直线 vs 圆盘），列为后续目标。

**机制（用户提出的两点）**：
1. **延长冻结睡眠**：`RECOVER_SLEEP` 60→**100 步（6s→10s）**。软锁车睡得更久，给协调层令牌车更多时间绕过它再醒来重新加入，避免醒得太早又卡进同一僵局。
2. **冻结车作为纯静止障碍物、且令牌车可贴身穿越**：把锁定/碰撞/排队中的"冻结车"从真实墙/障碍中**单独拆出**（`frozen_discs`）。原先它们混进 `all_static`、对所有车用 `STATIC_CLEAR+r=1.15m` 间隙——比活跃让路车的 `0.6m` 间隙还大，冻结车反而比会动的让路车更"挡路"，违背"锁定即静止障碍、方便令牌车通行"的设计初衷。现在令牌车对冻结车只留刚好高于碰撞阈值的 `1.05m` 圆心间距（仍远小于 `1.15m` 静态余量），既能贴身穿过、又绝不蹭上不能让路的冻结车。墙和真实障碍仍按完整余量正常避让。

**效果**：
- seed 12：2199 步碰撞（32 任务）→ **5000 步满程（41 任务）**，吞吐 +28%。
- 12-seed 扫描：满程存活 **5/12 → 8/12**。seeds 7、12、23、31、100 由碰撞/死锁转为满程存活；seed 42 新出现碰撞（973 步），列为下一靶子。

> 用户两点思路的逻辑：睡得久（给空间）+ 冻结车不再过度占道（令牌车能通行）——共同消解了复活时刻的硬碰撞，让 #14 零漏检的副作用基本收敛。

### #16 令牌车不原地转（尝试后放弃 —— 记录供参考）

**动机**：seed 777 COORD 死锁。诊断发现令牌车持令牌却 `v=0`：ORCA 把它的可行方向罚偏（周围障碍+车+墙），跟随器 `_steer_arch1` 见方向误差大就进入原地转向、输出 v=0，令牌车原地打转无法疏解 jam，最终三车全硬锁。

**尝试**：令牌车持令牌时禁止原地打转（误差 <120° 强制 arc-in）+ 给一个最低前进速度，逼它挪出去触发令牌轮换。

**为何放弃**：令牌车确实动起来了（777 从 DEADLOCK 变 collision），但**动起来就撞**——先撞墙（无条件最低速度把它怼进障碍），收敛参数后改撞让路车。深挖发现真因是**几何**：777 的 jam 发生在一条**实际仅 0.97m 宽**的左侧通道（被障碍 (2.77,13.78) 切出），两车（各直径 1m）对向通过物理上不可能；且让路车的退让方向恰好朝向令牌车（原路径往回退 = 朝令牌车走），ORCA 互相锁死。令牌机制选对了车，但选中的车在窄缝里无路可走。**结论**：这是几何过不去 + 退让方向对冲的问题，不是跟随器调参能解的。#16 代码已从分支移除，未合入。

### #17 几何重构 —— 改为真实差速车（外接圆模型）

**动机（用户提出）**：把抽象的 0.5m 圆形机器人换成一台真实的矩形差速车，用厂商手册参数，使 benchmark 贴近实车。

**车辆参数**：
- footprint 1.000 × 0.781 m；**外接圆半径 ≈ 0.635 m**（半对角线）——用外接圆是因为差速车会原地转到任意朝向，只有外接圆能保证任何朝向都不超出、不碰撞。真实矩形 footprint 留作后续。
- 轮距 0.569 m、轮半径 0.205 m（新增 `wheel_speeds(v,w)` 换算 helper）。
- V_MAX 0.9 m/s（max_vel_x）、W_MAX 1.0 rad/s（max_vel_theta）。
- **无最小转弯半径**：差速车可原地转。删除 R_MIN 约束，运动只受 V_MAX / W_MAX 限制（`_to_diff_drive`、`_steer_arch1` 相应去掉 R_min；arch2 保留一个纯外观平滑半径 `ARCH2_SMOOTH_R`）。
- max_vel_y = 0（非完整约束），结构上保证（全程无横向项）。

**配套调整**：`D_DEPLOY` 改为跟随 `2*ORCA_RADIUS`（出生间距随几何自适应，59 seed 实测零出生碰撞）；地图障碍尺寸注释更新。

**走廊拓宽（保持难度比例）**：0.635m 的车在原 5m 走廊里太挤（两车不碰需 1.27m，三车在窄段常不可行），实测 COORD 存活骤降到 2/12。为保持旧的"机器人/走廊"比例（旧：0.5m 车 / 5m 走廊 = 5:1），把 `MAP_W` 改为 **10× 车直径 ≈ 6.35m**；`INIT_X` / `INIT_ZONE` 随 `MAP_W` 自适应。

**结果（12-seed）**：拓宽后 COORD 存活恢复到 **6/12**（baseline 3/12）、平均任务 15.8，协调层明显有用（4 个 "COORD wins"）。比旧几何（8/12、~22 任务）仍略难，属合理范围。**黄金基线已在新几何上重建**；聚合阈值下调至新现实（min-survival 0.4、min-mean-tasks 12）。

> **重要**：#17 改变了 benchmark 的物理基准，旧 golden.json 作废、已重建。#17 不解决 777 的对冲死锁（那是几何 + 退让逻辑问题，见 #16），是独立的平台参数化工作。

### 仍可优化（下一步方向）

- **墙模型不一致（#15 暴露）**：目标采样器把墙当直线（`STATIC_CLEAR` 留白），碰撞判据把墙当圆盘（圆心间距）。靠墙目标（如 `x=4.3`）可能合法但实际贴着墙圆盘、近目标蠕动会蹭墙。应统一墙模型，或在采样器中也按圆盘留白。
- **seed 42 新碰撞**：#15 后唯一新增的回归，下一步定位。
- **碰撞（剩余）**：#15 后仍有少数 seed 碰撞（42、256），多与复活后乱冲/靠墙目标相关。
- 软锁定恢复目前是"原地重启"，复活后若环境没变会再次卡住、白睡一轮；可叠加"复活时随机微退一步"或"复活后短暂提权"加速脱困。
- jam 检测对"落单掉队"机器人无效（不构成 jam 就拿不到令牌），底部疏散后掉队者仍可能反复软锁。
- `STALL_TIMEOUT` / `PROGRESS_DELTA` / `RECOVER_SLEEP` / `HARD_LOCK_ATTEMPTS` / `JAM_CLEAR` 等参数均未系统调优。

---

### 未修复（保留供讨论，非 bug）

| # | 类型 | 说明 |
|---|------|------|
| 6 | 通用性局限 | 跳变（jam）检测是**两两配对**而非连通分量聚类。三机器人场景下永远正确；但若走廊出现两处互不相干的堵点，会被当成一处处理，只放行一个 holder，另一处无人管。与文档"congestion deadlock anywhere"的措辞有出入。三机器人下碰不到，建议要么改措辞，要么升级为 union-find 聚类。 |
| 7 | 文档不严谨 | `token_holder` 在存在多个 committed 机器人时按 `min(id)` 选 holder 而非 wait 时间。单 token 语义下通常只有一个 committed，属注释问题。 |
| 9 | 减速曲线 | `_orca_preferred` 用 `min(V_MAX, dist/DT)`，目标极近时给出小于 V_MAX 的速度，叠加 `_steer_arch1` 的 `compress` 项会双重减速。非 bug，但减速曲线非设计者可能预期的形状。 |
| 10 | 魔数 | 碰撞检测用 `COLL - 0.05`，即允许 5cm 重叠才判碰撞（有意宽容，避免 spawn 帧误判），但该 0.05m 未参数化。 |
| viz | 小问题 | `viz.coord_compare([单个seed])` 会因 `subplots(2,1)` 返回一维 axes 而 `TypeError`（本报告用独立脚本绕过，未改 viz.py）。 |

---

## 三、改动规模

- `coordination.py`：约 14 行变化（删 `deque` import、删 `reset_robot`、删 2 处 `coord_dyn_yield` 赋值、订正 1 处 docstring）。
- `container_sim.py`：死代码 + 防御性修复约 10 行（删 `coord_dyn_yield` 字段、删 `already_out`、#5 两处）；**#11 跟随器振荡修复**约 15 行（新增 `NEAR_TARGET_DIST` 常量 + `_steer_arch1` 近目标 arc-in 逻辑）。

死代码清理 + 防御性修复（#1-#5、#8）未改变任何实质导航/协调逻辑；**#11 是首个改变运动行为的修复**，它消除了控制器极限环，使机器人能精确到位完成任务。

死锁攻坚阶段的改动（见"二之二"）：
- `container_sim.py`：令牌降级斥力（改进 A，约 15 行）、#12 软锁定恢复（Robot 字段 + step_world 恢复逻辑 + run_sim 终止判定，约 25 行）、#13 低效死锁判据（约 12 行，后被 #14 取代）、**#14 进展看门狗**（Robot 看门狗字段 `best_goal_dist`/`stall_steps`/`pos_long` + 双腿判定替换旧死锁检测 + 各重置点，约 35 行）。
- `coordination.py`：改进 B 令牌轮换（重写 jam holder 选择 + 释放逻辑，约 25 行）。

这些改动改变了运动与调度行为，是为让协调层在 seed 12 上真正生效、并保证死锁零漏检而做的功能性增强。

---

## 四、复现结果（seed 12）

seed 12 是一张较难的地图：**2 个唯一路径瓶颈**（多数 seed 只有 1 个）。其第三个机器人（红/r2）原本受 #11 跟随器振荡影响全程 0 任务，修复 #11 后恢复正常。下表对比 #11 修复前后。

```
=== 修复 #11 前 ===
baseline: steps=805   all_locked   tasks=[2,2,0] total=4
COORD:    steps=5000  max_steps    tasks=[6,6,0] total=12

=== 修复 #11 后 ===
baseline: steps=1514  all_locked   tasks=[7,9,4] total=20
COORD:    steps=5000  max_steps    tasks=[7,5,11] total=23
```

| 指标 | baseline (#11后) | COORD (#11后) |
|------|------|------|
| 结束步数 | 1514（死锁） | 5000（存活满程） |
| 停止原因 | all_locked | max_steps |
| 完成任务总数 | 20 | 23 |
| 红色 r2 任务数 | 4（修复前 0） | 11（修复前 0） |
| 碰撞 | 0 | 0 |

**#11 阶段结论**：修复跟随器振荡后，协调层在双瓶颈难图上推掉死锁、吞吐提升约 3 倍。但深入分析（见"二之二"起点诊断）发现此时协调层实为空转，撑满 5000 步靠的是步数上限而非真正解围。

### seed 12 完整迭代演进

| 阶段 | COORD | baseline | 关键变化 |
|------|-------|----------|----------|
| #11 修复后 | 23（空转满程） | 20（1514步死锁） | 振荡消除，但调度空转 |
| + 改进A 令牌降级斥力 | 31（4016步死锁） | — | holder 不再被让路者卡死 |
| + 改进B 令牌轮换 | 36（4937步死锁） | — | 三方僵局可依次疏解 |
| + #12 软锁定恢复 | 37（5000步存活） | 39（5000步存活） | **首次彻底不死锁**，但 baseline 反超 |
| + #13 低效死锁判据 | 31（5000步存活） | 27（1374步碰撞） | COORD 反超 baseline |
| + **#14 进展看门狗（零漏检）** | **32（2199步碰撞）** | **28（5000步满程）** | 检测零漏检；副作用：复活碰撞增多 |

> #14 用进展看门狗替换了 #13 低效判据，把死锁检测做到零漏检（见"二之二 #14"）。代价是检测更灵敏、软锁恢复更频繁，复活时缺乏防撞协调导致 seed 12 在 2199 步碰撞退出。这暴露出独立的"恢复安全"问题，列为下一阶段首要项。

**当前结论**：经五步迭代，(1) 协调层从"空转"转为有意义的右行权管理；(2) **死锁检测达成零漏检**——所有真正卡死的机器人都被 `all_locked`/`collision` 捕获，无静默逃逸。当前待解决项是零漏检暴露的复活碰撞（见"仍可优化"）。

### 配套产物（最新）
- `seed12_watchdog.gif` —— #14 进展看门狗版 COORD 动画（当前版本）
- 早期阶段动画：`seed12_v5.gif`（低效判据版）、`seed12_coord_v3.gif`（令牌轮换版）、`seed12_softlock.gif`（软锁恢复版）

---

## 五、复现命令

```bash
# baseline
SEED=12 MAX_STEPS=5000 STEER_MODE=arch1 python container_sim.py

# 协调层
COORD=1 SEED=12 MAX_STEPS=5000 STEER_MODE=arch1 python container_sim.py
```
